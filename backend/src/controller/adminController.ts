import { Request, Response } from 'express';
import { ProductInterface } from '../model/productModel';

export async function productregistration(req: Request<{}, {}, ProductInterface>, res: Response) {
	
}